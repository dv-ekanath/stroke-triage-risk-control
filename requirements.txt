# Core — needed for the calibration track (runs with no dataset, no GPU)
numpy>=1.26
scipy>=1.11
pandas>=2.0
matplotlib>=3.8

# Needed only for the imaging track (Review 1 items 1.3, 1.4, 1.7)
nibabel>=5.2
scikit-learn>=1.4

# Needed only for the model-training track (Review 1 item 1.6+).
# Install in an isolated venv (Stroke-code/.venv) -- torch's CUDA build is
# version- and platform-specific and should not go in a shared interpreter.
#
# RTX 5060 is Blackwell (compute capability 12.0) and needs a cu128+ wheel;
# an older CUDA build silently falls back to CPU rather than erroring, so
# verify with `torch.cuda.is_available()` after install, not just import.
#   pip install torch --index-url https://download.pytorch.org/whl/cu128
# torch==2.11.0+cu128   (installed; pin exact version once training starts)
monai>=1.6
SimpleITK>=2.5
torchio>=1.2
tensorboard>=2.18
